<script>
    
    function linePlotlySvg(){
        /*
        var img_svg = d3.select('#svg-export');

        var names=<?php echo json_encode($labelswho); ?>;
        var values=<?php echo json_encode($datasetswho);  ?>;
        var trace1 = {
        x: names,
        y: values,
        type: 'scatter'
        };
        
        var layout = {
        autosize: false,
        width : 900, 
        height :600
        };

        var data = [trace1];


        Plotly.newPlot('myChart', data, layout).then( function(gd)
     {
       
        Plotly.toImage(gd,{format:'svg',height:900,width:900})
         .then(
             function(url)
         {
            img_svg.attr("src", url);
            Plotly.toImage(gd,{format:'svg',height:800,width:800});

         }
         )

         
         });
         */
}

</script>